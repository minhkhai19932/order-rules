---
files: "routes/pages/**/*"
---

# React Router

- Keep route definitions centralized (e.g., `src/routes/AppRoutes.tsx`).
- Use consistent auth gating pattern for protected routes (e.g., `ProtectedRoute`).
- Pages should be thin; heavy logic belongs in feature hooks/stores.
