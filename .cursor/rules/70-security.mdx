---
alwaysApply: true
---

# Security

- Never introduce hardcoded secrets/tokens/keys.
- Do not leak sensitive data in logs or error messages.
- Validate inputs at boundaries (API/forms) and handle failures safely.
