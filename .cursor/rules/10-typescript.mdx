---
alwaysApply: true
---

# TypeScript

- No `any`. Use `unknown` + type guards when necessary.
- Avoid unsafe `as` assertions unless justified with runtime checks.
- Public functions/hooks/stores must have clear input/output types.
- Prefer discriminated unions for complex UI states.
