---
alwaysApply: true
---

# Baseline

- Make the smallest correct change. No drive-by refactors.
- Follow existing project conventions (imports/exports/naming/folder structure).
- Do not add dependencies unless explicitly requested.
- If you change behavior, update related usage and tests (if tests exist).
