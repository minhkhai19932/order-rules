---
alwaysApply: true
---

# Output Format

- Default output: provide code changes as complete file contents for changed files (or unified diff if requested).
- If the user asks "commands only", output only runnable shell commands.
- Never paste secrets. If sensitive info is needed, use env vars and placeholders.
